apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
  namespace: split-learning
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
  template:
    metadata:
      labels:
        app: server
    spec:
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
      nodeSelector:
        kubernetes.io/hostname: controlplane
      containers:
        - name: server
          image: minhbui1/server:v2.0.2
          command: ["sh", "-c", "sleep 10 && python -u server.py"]
          volumeMounts:
            - name: config-volume
              mountPath: /app/config.yaml
              subPath: config.yaml
            - name: server-result
              mountPath: /app/result
      volumes:
        - name: config-volume
          configMap:
            name: split-learning-config
        - name: server-result
          persistentVolumeClaim:
            claimName: server-result-pvc
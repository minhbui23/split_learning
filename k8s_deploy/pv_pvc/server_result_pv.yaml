apiVersion: v1
kind: PersistentVolume
metadata:
  name: server-result-pv
spec:
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: "/mnt/k8s/data/server"